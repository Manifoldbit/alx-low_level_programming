#include "main.h"
#include <string.h>

/**
 * strcpy - copy the string
 * @dest: destination string
 * @src: the source string whic is to be copied
 * Return: a copied character
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
